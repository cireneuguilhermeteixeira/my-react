# My-react

A minimal React-like library implemented from scratch with zero dependencies.

Goals:

- JSX-free initial prototype (manual `createElement` calls)
- Function components and props
- Simple DOM rendering

Next steps:

- Internal "fiber" tree
- Hooks (`useState`, `useEffect`, etc.)
- Context support (`createContext`, `useContext`)
